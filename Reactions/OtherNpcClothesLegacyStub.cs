// Legacy compatibility stub.
//
// Older test builds of Outfit Compliments had a file that is no longer needed.
// If this source package is extracted over an existing project folder, Visual
// Studio may keep compiling that old file and fail because the related config
// fields were removed. Keep this file empty on purpose so extracting the new
// source over the old project replaces the obsolete implementation with a
// harmless stub.
